<!-- Devices table -->
<div class="row">
  <div class="col s12">
    <% if @supply_devices.any? %>
      <ul class="collapsible popout left-align" data-collapsible="expandable">
        <li>
          <div class="collapsible-header active">
            <i class="material-icons">print</i>
            Assigned Devices
            <span class="right"><%= @supply_devices.count %></span>
          </div>

          <div class="collapsible-body" style="margin-left: 20px; margin-right: 20px;">

            <%= table_for_devices(@supply_devices) %>

          </div>
        </li>
      </ul>
    <% end %>
  </div>
</div>