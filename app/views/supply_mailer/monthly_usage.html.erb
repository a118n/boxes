<h3>Supplies usage in <%= @site.name %> for <%= @month %> <%= @year %></h3>
<br>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Vendor</th>
      <th>Description</th>
      <th>Used</th>
    </tr>
  </thead>
  <tbody>
    <% @supplies.each do |supply| %>
      <tr>
        <td><%= link_to supply.name, site_supply_url(supply.site, supply) %></td>
        <td><%= supply.vendor %></td>
        <td><%= supply.description %></td>
        <td><%= supply.used %></td>
      </tr>
    <% end %>
  </tbody>
</table>